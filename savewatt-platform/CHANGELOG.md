# Changelog

## 2026-09-25 — Production deploy + Cloudflare Email Service dual-driver + webhook auth fix

- Deployed to production (worker version `79a7cc19-d8b6-44e5-90e8-4b3132850927`, app.savewatt.fr); remote D1 migrated through `0015_notifications` (0005–0014 backlog applied 2026-09-24), R2 bucket `savewatt-documents` (EU) bound, `CRON_SECRET` / `PORTAL_TOKEN_SECRET` set.
- Added the Cloudflare Email Service `send_email` binding (`EMAIL`) and a dual-driver transactional sender (`src/lib/email/transactional-email.ts`): EMAIL binding preferred, Resend fallback when `RESEND_API_KEY` is set, explicit skip when neither is configured, no cross-driver retry; attachments supported on both drivers (explicit capability map, tested).
- Offer delivery (both PDFs attached) and the follow-up sweep now route through the dual-driver sender.
- Fixed webhook/cron auth bypass: `/api/webhooks/resend` and `/api/cron/followups` are public in the Clerk middleware and authenticate themselves (svix / Bearer CRON_SECRET); restored the Resend webhook route with svix `verifyResend` and `deliveryStateForResendEvent` mapping onto the delivery tracker.
- Regenerated `cloudflare-env.d.ts` (EMAIL binding; wrangler vars now literal-typed — adjusted the Tremendous sandbox test override accordingly).
- Smoke-verified live: Resend webhook 503 `WEBHOOK_NOT_CONFIGURED` (until the secret is supplied), cron 401 from its own Bearer check, portal invalid-link 200, API walls 401. Signed-out page 404 via Clerk dev-browser rewrite remains expected until Clerk production keys are supplied.
- 61 lib tests green, i18n parity green, lint clean, production build green.

## 2026-09-23 — Symphonics budget engine, dual offer PDFs, operator pricing/margin settings

- Documented the deduced Symphonics pricing model in `specs/symphonics-pricing-model.md` (validated formula, euro-exact reconciliation against the real Josh proposal, variable visibility matrix, contract mechanics reference).
- Added migration `0014` (local D1 only; remote pending deploy): versioned org-scoped `pricing_parameters` (CEE, capacity, accise, CTA, TVA, TURPE fixed/variable) seeded with validated constants, `margin_grids.role_scope` (ADMIN/REGIE), and `offer_versions.budget_json` + marketing PDF columns.
- Added the pure estimate engine (`src/lib/offers/estimate.ts`): budget prévisionnel computation and per-cadran annual volume forecasting; `createOfferVersion` now stores the customer-safe budget snapshot.
- Added régie visibility serialization (`offer-visibility.ts`): buy price, CEE/capacity components, margin, and grid bounds never leave the server for régie roles; margin grids and pricing parameters APIs 403 for non-operator roles.
- Added dual PDF rendering: Symphonics-style budget prévisionnel + marketing one-pager, both rendered through the BROWSER binding, archived in R2 with SHA-256 each, and attached to the Resend delivery email (marketing first); `GET /api/crm/offer-versions/[id]/pdf?kind=marketing`.
- Added operator settings: writable margin grids (ADMIN + REGIE scopes) and new pricing-parameters screen; nav entries hidden for régie roles.
- Replayed the real Josh dossier end-to-end on local D1: margin-0 version reconciles exactly to Symphonics' 16 812 € TTC; default-margin version yields 17 561 € TTC/an with 2 238 €/an savings (artifacts in `tmp/pdf-analysis/output/`).
- Added budget reconciliation, forecast, régie serializer, and dual-template secrecy tests. 46 lib tests green, i18n parity green, lint and production build green.

## 2026-09-22 — Primary workflow: Excel lead import → durable extraction → immutable offer → PDF email delivery

- Added leads UI (`/leads`): list, status filter, single-lead creation, conversion, and Excel bulk import with a downloadable French template (`public/templates/modele-import-prospects.xlsx`, generated by `scripts/generate-lead-template.mjs`).
- Added `lead_imports` migration and `POST /api/crm/leads/import`: per-row validation, scoped dedup against leads/clients (SIREN/email/phone), idempotent replays, and row-by-row result reports.
- Added `extractions` migration and scoped extraction APIs: run Gemini against a private R2 document (`POST /api/crm/documents/[id]/extract`), persist results, and save human-validated fields (`PUT /api/crm/extractions/[id]`) with dossier timeline events.
- Added `supplier_offers`, `margin_grids`, `offer_versions`, and `offer_deliveries` migrations with scoped repositories.
- Supplier offer intake now persists to D1 in connected mode (manual per-cadran entry; CSV connector stays disabled).
- Added server-enforced margin resolution: default from the effective versioned grid, in-bounds overrides require a reason, out-of-bounds overrides create `APPROVAL_REQUIRED` versions approvable only by admins.
- Added immutable offer versions with canonical SHA-256 snapshots (validated contract + supplier offer + margin + comparison + client price lines); regenerated versions never mutate sent ones; dossier advances `analyzed → proposalReady`.
- Added customer-safe A4 PDF generation through the Cloudflare Browser Rendering binding, archived privately in R2 with hash + template version on the offer version.
- Added Resend delivery with PDF attachment, idempotency-keyed `offer_deliveries`, `SENT` transitions, dossier `proposalReady → sent`, and timeline events.
- Extended the dossier state machine (`proposalReady → sent`) and the CRM error vocabulary (`OFFER_INPUT_MISSING`, `OFFER_MARGIN_GRID_MISSING`).
- Added lead-import parser tests and offer-engine tests (margin-aware comparator, snapshot hash stability, PDF secrecy of buy price/margin). 34 lib tests green.

## 2026-09-19 — Account approval and organization hierarchy

- Added durable organization status/limits, membership versions, and expiring organization invitations.
- Added branch-scoped organization creation, suspension, member suspension/reactivation, and invitation APIs with parent-limit and role-level enforcement.
- Added a super-admin access console for partner/customer registration decisions and internal operator/finance allowlist management.
- Added verified-email invitation activation, optimistic concurrency, audit events, and ancestor-suspension enforcement across descendant logins.
- Replaced the organization and branch-user demo screens with operational D1-backed management surfaces while retaining an explicit non-mutating preview mode.

## 2026-09-19 — Private dossier documents

- Added a private Cloudflare R2 binding and durable document lifecycle for bills, current contracts, and supplier offers.
- Added authenticated, tenant-scoped upload, listing, and download APIs with 10 MB limits, MIME and magic-byte checks, SHA-256 metadata, safe filenames, and no-store downloads.
- Connected real dossier creation and dossier detail screens to private document storage while retaining the explicitly marked demo path.
- Added compensating object cleanup, document validation tests, and bilingual upload states without changing DocuSeal, signing, or proposal delivery.

## 2026-09-18 — Durable CRM foundation

- Added the first durable CRM migration with leads, dossier activity events, tasks, and optimistic record versions.
- Added fail-closed organization-path and owner-scoped CRM repositories for lead creation/conversion, dossier queries/status changes, timelines, and tasks.
- Added authenticated CRM APIs for leads, atomic lead conversion, dossiers, activity, and task state.
- Added validation, authorization, and dossier state-machine regression tests without changing DocuSeal or signing behavior.

## 2026-09-18

- Replaced the previous gifting integration with Tremendous sandbox order issuance, signed webhook intake, D1 provider migration, and Cloudflare Worker secret configuration.

## 2026-09-17

- Added the repository-grounded platform execution blueprint covering the admin, régie, and customer portals; durable CRM; proposal/PDF delivery; configuration; commissions; and production acceptance gates.

## 2026-09-16

- Rebuilt authentication as a dedicated branded route group with Clerk.
- Added role-scoped shells and screens for operator, finance, régie, back-office, team, apporteur, and customer users.
- Added organization, workflow, margin, commission, document, branding, and session settings surfaces.
- Added bill extraction validation and manual supplier-offer intake screens.
- Added authenticated DocuSeal submission creation, dossier binding, email 2FA, and a fail-safe webhook relay seam.
- Added the locked 66% network commission calculation and automated tests.
- Aligned platform tokens, typography, logo, and legal document footer with SaveWatt / AX TECH.
- Created the production Cloudflare D1 database in Western Europe and applied the initial multi-tenant, dossier, signature, commission, wallet, gifting, webhook, and audit schema.
- Added OpenNext/Workers configuration and generated a typed `DB` binding.
- Added provider-backed reward issuance with Clerk-derived recipients, D1 credit reservations, idempotent provider orders, failure rollbacks, and signed webhook intake.
- Hardened DocuSeal webhooks with timestamped HMAC verification and authoritative D1 signature updates.
- Added a guarded DocuSeal PDF-template uploader that refuses to run until the source is explicitly confirmed sanitized.
- Added segmented customer and partner self-registration with a D1-backed pending-access queue.
- Removed the unsafe default apporteur role: dashboard access now requires an active D1 membership.
- Added a mandatory D1 email whitelist for internal SaveWatt operator and finance accounts.
- Configured the Cloudflare Worker custom domain at `app.savewatt.fr`.
- Removed the landing-page theme switcher and connected every `Connexion` link to the hosted application.
- Split customer and partner login selection onto `app.savewatt.fr` and internal access onto `admin.savewatt.fr`.
- Enforced hostname-level role separation so internal accounts cannot enter the customer app and external accounts cannot enter administration.
