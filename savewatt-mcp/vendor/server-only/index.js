// Local stub: platform modules import "server-only" as a Next.js bundling guard.
// Inside this Worker the guard is meaningless (everything here is server-side),
// so it resolves to a no-op module.
export {};
